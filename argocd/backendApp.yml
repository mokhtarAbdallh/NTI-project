# argocd/application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend-app
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/mokhtarAbdallh/NTI-project
    targetRevision: master
    path: k8s/backend   # 👈 backend فقط

  destination:
    server: https://kubernetes.default.svc
    namespace: micro-prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
